bundle:
  name: my-simple-bundle

targets:
  prod:
    workspace:
      host: ${{ secrets.DATABRICKS_HOST }}
    resources:
      jobs:
        my_first_job:
          name: my-first-bundle-job
          tasks:
            - task_key: run_notebook
              notebook_task:
                notebook_path: ./src/my_notebook.py